name: Test Minimal Reusable

on:
  workflow_call:
    inputs:
      service_name:
        required: true
        type: string
      service_folder:
        required: true
        type: string
      needs_db:
        required: false
        type: boolean
        default: false

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello ${{ inputs.service_name }} ${{ inputs.service_folder }} ${{ inputs.needs_db }}"
